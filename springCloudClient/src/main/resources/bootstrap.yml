server:
  port: 8484

spring:
  profiles:
    active: real
  application:
    name: client
  cloud:
    config:
      uri: http://localhost:8282

endpoints:
  refresh:
    enabled: true

management:
  security:
    enabled: false